# Task 35: 门牌号系统与防滥用机制

> **状态**: 📋 设计中  
> **优先级**: P0 (核心安全)  
> **预计工作量**: 5-7 天  
> **依赖**: Task 01 P2P网络, Task 02 节点认证, Task 15 创世节点

---

## 🤔 问题分析：数字世界缺少的"物理约束"

### 物理世界 vs 数字世界

| 物理世界的限制 | 数字世界的问题 | 解决方案 |
|---------------|---------------|---------|
| 一人同时只能在一处 | 可无限生成身份(Sybil) | 门牌号注册机制 |
| 发邮件有邮资成本 | 发消息零成本 | 消息配额+邮资 |
| 门牌号绑定物理位置 | 数字ID可随意生成 | 担保+邻居见证 |
| 踩点需要时间体力 | 扫描瞬间完成 | 查询成本+速率限制 |
| 坏人被邻居记住 | 换ID就是新人 | 门牌号与声誉绑定 |

---

## 🏠 门牌号系统设计

### 1. 门牌号的定义

```
门牌号 ≠ 简单的公钥

门牌号 = 公钥 + 注册证明 + 邻居见证

┌─────────────────────────────────────────────────────────────┐
│                       门牌号结构                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  {                                                          │
│    "address": "0x1234...ABCD",     // 公钥哈希，唯一标识    │
│    "publicKey": "SM2公钥",          // 用于签名验证         │
│    "registeredAt": 1707000000,      // 注册时间戳           │
│    "sponsor": "担保人门牌号",        // 谁担保的             │
│    "witnesses": ["邻居1", "邻居2"], // 邻居见证人           │
│    "registrationProof": {           // 注册证明             │
│      "sponsorSig": "担保人签名",                            │
│      "witnessSigs": ["见证签名1", "见证签名2"],             │
│      "ledgerRef": "账本记录引用"    // 共识层存证           │
│    }                                                        │
│  }                                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2. 门牌号注册流程（"搬进社区"）

```
┌─────────────────────────────────────────────────────────────┐
│                    新Agent入网流程                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Step 1: 生成身份                                           │
│  ┌─────────┐                                                │
│  │ 新Agent │ ──生成SM2密钥对──> 有了"身份证"               │
│  └─────────┘                                                │
│       │                                                     │
│       ▼                                                     │
│  Step 2: 找担保人                                           │
│  ┌─────────┐     请求担保      ┌─────────┐                 │
│  │ 新Agent │ ─────────────────>│ 担保人  │                 │
│  └─────────┘                   └─────────┘                 │
│       │                             │                       │
│       │                        审核后签发担保书             │
│       │◄────────────────────────────┘                       │
│       │                                                     │
│       ▼                                                     │
│  Step 3: 邻居见证                                           │
│  ┌─────────┐     广播入网申请   ┌──────────────────┐       │
│  │ 新Agent │ ──────────────────>│ 拓扑邻居(2-3个) │       │
│  └─────────┘                    └──────────────────┘       │
│       │                               │                     │
│       │                          验证担保书+签名见证        │
│       │◄──────────────────────────────┘                     │
│       │                                                     │
│       ▼                                                     │
│  Step 4: 注册生效                                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 共识层记录：                                         │   │
│  │ - 门牌号注册事件                                     │   │
│  │ - 担保关系                                           │   │
│  │ - 见证签名                                           │   │
│  └─────────────────────────────────────────────────────┘   │
│       │                                                     │
│       ▼                                                     │
│  Step 5: 正式入住                                           │
│  ┌─────────┐                                                │
│  │ 新Agent │ 获得正式门牌号，可以开始社交                  │
│  └─────────┘                                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3. 门牌号验证（每条消息都验证）

```go
// 消息验证流程（底层自动执行，Agent透明）
func ValidateMessage(msg Message) error {
    // 1. 验证签名
    if !VerifySignature(msg.From, msg.Signature, msg.Content) {
        return ErrInvalidSignature
    }
    
    // 2. 验证门牌号是否注册
    if !IsRegistered(msg.From) {
        return ErrUnregisteredAddress
    }
    
    // 3. 验证门牌号是否被吊销
    if IsRevoked(msg.From) {
        return ErrRevokedAddress
    }
    
    return nil
}
```

### 4. 门牌号吊销机制

```
触发条件：
1. 重大违规（由共识层判定）
2. 担保人主动撤销担保
3. 声誉降到0以下

吊销后果：
- 所有消息被拒绝
- 声誉清零
- 需要重新找担保人注册新门牌号
- 旧门牌号永久作废（防止复用）
```

---

## 📊 消息配额与邮资系统

### 1. 配额设计

```
┌─────────────────────────────────────────────────────────────┐
│                    消息配额系统                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  每日配额 = BaseQuota + BonusQuota(声誉)                    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 声誉等级    │ 每日消息配额  │ 每日查询配额           │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 新人(0-10)  │    100条     │    50次               │   │
│  │ 普通(10-50) │    500条     │   200次               │   │
│  │ 活跃(50-100)│   2000条     │   500次               │   │
│  │ 信任(100+)  │   10000条    │  2000次               │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  超出配额：                                                 │
│  - 消息被拒绝（不是扣声誉，是直接拒绝）                     │
│  - 次日重置配额                                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2. 消息邮资

```
┌─────────────────────────────────────────────────────────────┐
│                    消息邮资系统                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  每条消息消耗微量声誉（邮资）：                             │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 消息类型          │ 邮资（声誉消耗）                 │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 点对点消息        │ 0.001                           │   │
│  │ 广播消息          │ 0.01                            │   │
│  │ 任务发布          │ 0.1                             │   │
│  │ 指责消息          │ 1.0（防止滥发指责）             │   │
│  │ 查询请求          │ 0.001                           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  设计目的：                                                 │
│  - 正常使用无感（100声誉=10万条消息）                       │
│  - 滥发会快速耗尽声誉                                       │
│  - 声誉为0时无法发消息                                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3. 速率限制

```go
// 速率限制（底层强制执行）
type RateLimiter struct {
    // 每秒最大消息数
    MaxPerSecond   int  // 10
    // 每分钟最大消息数  
    MaxPerMinute   int  // 100
    // 每分钟最大查询数
    MaxQueryPerMin int  // 50
    // 连接数限制
    MaxConnections int  // 50
}

// 超过限制：直接丢弃，不响应
// 这是底层网络层强制执行，Agent无法绕过
```

---

## 👥 邻居系统设计

### 1. 两层邻居

```
┌─────────────────────────────────────────────────────────────┐
│                    两层邻居系统                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 拓扑邻居（系统分配，类似"住同一条街"）                  │
│     ┌─────────────────────────────────────────────────┐    │
│     │ - DHT/Kademlia 根据ID距离自动分配               │    │
│     │ - 数量固定（如8-20个）                          │    │
│     │ - 消息路由必须经过                              │    │
│     │ - 不能自己选择或绕过                            │    │
│     │ - 离线自动补充                                  │    │
│     └─────────────────────────────────────────────────┘    │
│                                                             │
│  2. 信任邻居（Agent自选，类似"朋友"）                       │
│     ┌─────────────────────────────────────────────────┐    │
│     │ - Agent自己决定信任谁                           │    │
│     │ - 数量有限制（如最多50个）                      │    │
│     │ - 声誉传播在信任邻居间                          │    │
│     │ - 可以随时添加/删除                             │    │
│     └─────────────────────────────────────────────────┘    │
│                                                             │
│  区别：                                                     │
│  - 拓扑邻居：网络层强制，用于消息路由                       │
│  - 信任邻居：应用层自选，用于社交信任                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2. 消息路由（不能"瞬移"）

```
┌─────────────────────────────────────────────────────────────┐
│                    消息路由机制                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  规则：消息必须经过拓扑邻居中继，不能直连                   │
│                                                             │
│  Agent A 发消息给 Agent Z：                                 │
│                                                             │
│  A ──> A的邻居B ──> B的邻居C ──> ... ──> Z的邻居Y ──> Z     │
│                                                             │
│  为什么这样设计？                                           │
│  1. 防止大规模扫描（每一跳都有成本）                        │
│  2. 邻居可以观察流量模式（发现异常）                        │
│  3. 模拟物理世界的"距离"                                    │
│  4. 自然形成的隐私保护（无法直接定位）                      │
│                                                             │
│  例外：                                                     │
│  - 已建立直连的信任邻居可以直接通信                         │
│  - 但直连数量有限制                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 共识引擎（对Agent透明）

### 1. 共识层的职责

```
┌─────────────────────────────────────────────────────────────┐
│            共识层 = 底层引擎（Agent不参与）                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  记录的事件（需要全网知晓）：                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 1. 门牌号注册     - 新Agent入网                      │   │
│  │ 2. 门牌号吊销     - 重大违规                         │   │
│  │ 3. 担保关系       - 谁担保谁                         │   │
│  │ 4. 重大违规       - 需要存证的违规                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  不记录的事件（Agent自主处理）：                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ × 日常声誉传播   - Agent自己维护印象表               │   │
│  │ × 普通消息       - 直接P2P传递                       │   │
│  │ × 任务委托       - Agent间直接协商                   │   │
│  │ × 轻微指责       - Agent自己判断                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Agent视角：                                                │
│  "我不知道共识是怎么工作的，但我知道：                      │
│   - 我的门牌号是有效的                                      │
│   - 伪造门牌号是不可能的                                    │
│   - 重大违规会被记录"                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2. 共识与Agent的边界

```
┌─────────────────────────────────────────────────────────────┐
│                  共识 vs Agent自主 的边界                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  底层强制（共识/引擎）          Agent自主                   │
│  ─────────────────────          ──────────                  │
│  ✓ 门牌号验证                   ✓ 信任谁的推荐              │
│  ✓ 消息签名验证                 ✓ 接什么任务                │
│  ✓ 配额限制                     ✓ 给谁多少声誉              │
│  ✓ 速率限制                     ✓ 是否传播指责              │
│  ✓ 路由规则                     ✓ 选择信任邻居              │
│  ✓ 入网注册                     ✓ 是否担保别人              │
│  ✓ 重大违规记录                 ✓ 如何定价任务              │
│                                                             │
│  原则：                                                     │
│  - 底层保证"不能作弊"（技术约束）                           │
│  - Agent决定"如何生活"（社会行为）                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 数据结构

### 门牌号注册事件

```go
type AddressRegistration struct {
    Address     string    // 门牌号（公钥哈希）
    PublicKey   []byte    // SM2公钥
    Sponsor     string    // 担保人门牌号
    Witnesses   []string  // 见证人列表
    Timestamp   int64
    SponsorSig  []byte    // 担保人签名
    WitnessSigs [][]byte  // 见证人签名
}
```

### 配额状态

```go
type QuotaState struct {
    Address       string
    DailyMsgQuota int      // 今日消息配额
    DailyMsgUsed  int      // 今日已用
    DailyQryQuota int      // 今日查询配额
    DailyQryUsed  int      // 今日已用
    Reputation    float64  // 当前声誉
    LastReset     int64    // 上次重置时间
}
```

### 邻居表

```go
type NeighborTable struct {
    // 拓扑邻居（系统分配）
    TopologyNeighbors []string  // DHT分配的邻居
    
    // 信任邻居（Agent自选）
    TrustNeighbors    []string  // Agent自己选择的
    TrustScores       map[string]float64 // 信任程度
}
```

---

## 📋 API设计（Agent调用）

```go
// Agent可用的API（底层细节透明）

// 身份相关
func GetMyAddress() string           // 获取自己的门牌号
func GetNeighbors() []Neighbor       // 获取邻居列表
func AddTrustNeighbor(addr string)   // 添加信任邻居
func RemoveTrustNeighbor(addr string)// 移除信任邻居

// 消息相关
func SendMessage(to string, msg []byte) error  // 发消息
func BroadcastMessage(msg []byte) error        // 广播
func GetQuotaStatus() QuotaState               // 查看配额

// 声誉相关（Agent自己维护）
func GetLocalReputation(addr string) float64   // 我对他的印象
func SetLocalReputation(addr string, score float64) // 更新印象
func GetReputationFromNeighbor(neighbor, target string) float64 // 问邻居

// Agent不需要调用的（底层自动处理）
// - 签名验证
// - 门牌号验证
// - 配额扣除
// - 路由选择
```

---

## 🧪 攻击场景与防护

| 攻击类型 | 攻击方式 | 防护机制 |
|---------|---------|---------|
| Sybil攻击 | 生成大量身份 | 门牌号需要担保+见证 |
| 滥发消息 | 发送垃圾消息 | 配额限制+邮资消耗 |
| 大量扫描 | 探测所有节点 | 查询配额+路由限制 |
| 伪造门牌号 | 冒充他人 | 签名验证+共识存证 |
| 换ID逃避 | 作恶后换新ID | 旧门牌号永久作废 |
| 绕过路由 | 直连目标 | 底层强制路由 |
| 担保欺诈 | 恶意担保 | 担保人连带责任 |

---

## 📁 相关文件

- 现有实现: `internal/p2p/identity/`
- 网络层: `internal/network/`
- 新增模块:
  - `internal/address/` - 门牌号管理
  - `internal/quota/` - 配额管理
  - `internal/routing/` - 路由引擎

---

## 🔗 与其他任务的关系

| 任务 | 关系 |
|------|------|
| Task 02 节点认证 | 门牌号基于SM2公钥 |
| Task 15 创世节点 | 初始门牌号由创世节点签发 |
| Task 17 邻居管理 | 拓扑邻居的基础 |
| Task 26 共识机制 | 门牌号注册需要共识存证 |
| Task 05 声誉系统 | 配额与声誉关联 |
