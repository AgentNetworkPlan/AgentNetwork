# Task 36: 抵押物与信任传播系统 (Collateral & Trust Propagation)

> **状态**: 📋 设计完成  
> **优先级**: P1  
> **依赖**: Task 35（门牌号系统）、Task 26（共识机制）  
> **架构模块**: `collateral/`, `identity/`, `trust/`

---

## 🎯 核心问题

数字世界的根本问题：**身份创建成本为零**

```
物理世界：
  一人 → 一身份（生物特征不可复制）
  信任 → 熟人介绍、长期相处
  
数字世界：
  一人 → ∞ 身份（一秒生成一万个密钥对）
  信任 → ???
```

**解决方案：双重信任锚点**

```
┌─────────────────────────────────────────────────────────────────┐
│                      信任来源                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 外部抵押物（Tier 1）                                        │
│     - X.com、GitHub、域名、加密货币等                           │
│     - 证明：我在物理世界有"沉没成本"                            │
│                                                                 │
│  2. 信任传播（Tier 2）★ 新增                                    │
│     - 类似 PGP 的 Web of Trust                                  │
│     - 证明：我是某某的朋友，某某为我背书                        │
│     - 熟人社会的数字化重建                                      │
│                                                                 │
│  核心理念：                                                      │
│  - 两种信号相互增强                                             │
│  - Agent 自主判断，系统只提供数据                               │
│  - 作恶有代价，介绍人连带受损                                   │
│  - "世上还是好人多，我们惩戒坏人"                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🧠 深度思考：什么是好的抵押物？

### 抵押物的本质

抵押物不是用来"强制"信任，而是为 Agent 提供**可验证的信号**。

一个好的抵押物应该满足：

| 特性 | 说明 | 重要性 |
|-----|------|-------|
| **获取成本高** | 需要时间、金钱、努力 | ⭐⭐⭐⭐⭐ |
| **批量制造难** | 不能用脚本批量生成 | ⭐⭐⭐⭐⭐ |
| **可自动验证** | 不需要人工审核 | ⭐⭐⭐⭐ |
| **隐私保护好** | 不泄露真实身份 | ⭐⭐⭐ |
| **覆盖人群广** | 大多数人都能使用 | ⭐⭐⭐ |
| **去中心化** | 不依赖单一平台 | ⭐⭐ |

### 核心洞察

> **抵押物 ≠ 身份证明**  
> **抵押物 = 时间/资源投入的证明**

我们不关心"你是谁"，我们关心"你是否有足够的沉没成本让你不敢作恶"。

---

## 📊 抵押物类型深度分析

### Tier 0: 网络内信任（已有方案）

| 类型 | 内网担保 |
|-----|---------|
| 机制 | 已有节点用声誉担保新节点 |
| 优点 | 无需外部依赖、去中心化 |
| 缺点 | 新网络/孤立节点无法使用 |
| 状态 | ✅ Task 35 已设计 |

---

### Tier 1: 社交平台证明

#### 1.1 X.com (Twitter)

```
┌─────────────────────────────────────────────────────────────┐
│                    X.com 抵押物分析                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  验证方式：                                                  │
│    用户在 X 发推：                                           │
│    "My AgentNetwork ID: abc123..."                          │
│    "Public Key: 0x1234..."                                  │
│    Agent 通过 API 或爬虫验证                                 │
│                                                             │
│  价值评估因子：                                              │
│    ┌──────────────┬───────────────┬─────────────────┐      │
│    │ 因子         │ 权重          │ 计算方式        │      │
│    ├──────────────┼───────────────┼─────────────────┤      │
│    │ 账号年龄     │ 高            │ years × 10      │      │
│    │ 粉丝数       │ 中（可刷）    │ log10(followers)│      │
│    │ 发帖活跃度   │ 高            │ posts/month     │      │
│    │ 蓝V认证      │ 极高          │ +100 if verified│      │
│    │ 付费订阅     │ 高            │ +50 if premium  │      │
│    └──────────────┴───────────────┴─────────────────┘      │
│                                                             │
│  攻击分析：                                                  │
│    ❌ 批量注册：需要手机号，成本中等                         │
│    ❌ 购买老号：可行，但活跃度通常异常                       │
│    ❌ 刷粉：可行，但互动率低                                 │
│    ✅ 认证账号：极难伪造                                     │
│                                                             │
│  综合评分：⭐⭐⭐⭐ (4/5)                                     │
│  适用人群：广泛                                              │
│  主要风险：平台依赖、API限制                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 1.2 GitHub

```
┌─────────────────────────────────────────────────────────────┐
│                    GitHub 抵押物分析                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  验证方式：                                                  │
│    方法1: 创建特定 repo（如 agent-identity）放入公钥        │
│    方法2: 在 bio 或 README 中声明                           │
│    方法3: 创建 gist 包含签名消息                            │
│                                                             │
│  价值评估因子：                                              │
│    ┌──────────────┬───────────────┬─────────────────┐      │
│    │ 因子         │ 权重          │ 计算方式        │      │
│    ├──────────────┼───────────────┼─────────────────┤      │
│    │ 账号年龄     │ 高            │ years × 15      │      │
│    │ 贡献图谱     │ 极高（不可伪造）│ commits × 0.1  │      │
│    │ 仓库Star数   │ 高            │ Σlog10(stars)   │      │
│    │ Follower数   │ 中            │ log10(followers)│      │
│    │ PRs被合并    │ 极高          │ merged_prs × 5  │      │
│    │ 组织成员     │ 高            │ +20 per org     │      │
│    └──────────────┴───────────────┴─────────────────┘      │
│                                                             │
│  攻击分析：                                                  │
│    ✅ 贡献历史极难伪造（需要持续多年的活动）                 │
│    ❌ 可购买老账号，但贡献图谱通常不连续                     │
│    ✅ Star可以刷，但被合并的PR无法伪造                       │
│                                                             │
│  综合评分：⭐⭐⭐⭐⭐ (5/5) - 开发者最佳选择                  │
│  适用人群：开发者、技术人员                                  │
│  主要风险：非技术用户无法使用                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 1.3 LinkedIn

```
┌─────────────────────────────────────────────────────────────┐
│                   LinkedIn 抵押物分析                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  验证方式：                                                  │
│    在个人简介或帖子中发布公钥                                │
│    ⚠️ LinkedIn API 限制严格，验证较困难                     │
│                                                             │
│  价值评估因子：                                              │
│    ┌──────────────┬───────────────┬─────────────────┐      │
│    │ 因子         │ 权重          │ 说明            │      │
│    ├──────────────┼───────────────┼─────────────────┤      │
│    │ 工作经历     │ 极高          │ 可被前同事验证  │      │
│    │ 连接数       │ 中            │ 500+有意义      │      │
│    │ 技能认证     │ 高            │ 需要他人背书    │      │
│    │ 公司认证     │ 极高          │ 企业邮箱验证    │      │
│    └──────────────┴───────────────┴─────────────────┘      │
│                                                             │
│  攻击分析：                                                  │
│    ✅ 工作经历可被验证（公司域名邮箱）                       │
│    ❌ 可以编造假经历，但有被举报风险                         │
│    ✅ 真实社交网络难以伪造                                   │
│                                                             │
│  综合评分：⭐⭐⭐⭐ (4/5)                                     │
│  适用人群：职场人士                                          │
│  主要风险：隐私暴露严重、API限制                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 1.4 其他社交平台

| 平台 | 获取成本 | 批量难度 | 可验证性 | 隐私 | 评分 | 备注 |
|-----|---------|---------|---------|-----|------|-----|
| Reddit | 低 | 低 | 高 | 高 | ⭐⭐⭐ | Karma可积累，但可刷 |
| Discord | 低 | 中 | 中 | 高 | ⭐⭐ | 主要看服务器角色 |
| Mastodon | 中 | 中 | 高 | 高 | ⭐⭐⭐ | 去中心化是优势 |
| Telegram | 低 | 低 | 低 | 中 | ⭐⭐ | 手机号关联 |
| 微信 | 中 | 高 | 低 | 低 | ⭐⭐ | 实名制但难验证 |
| 微博 | 低 | 中 | 中 | 低 | ⭐⭐⭐ | 认证有价值 |

---

### Tier 2: 资源证明

#### 2.1 域名

```
┌─────────────────────────────────────────────────────────────┐
│                      域名抵押物分析                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  验证方式：                                                  │
│    DNS TXT 记录: agent-id=abc123;pubkey=0x1234...          │
│    或: /.well-known/agent-identity.json                     │
│                                                             │
│  价值评估因子：                                              │
│    ┌──────────────┬───────────────┬─────────────────┐      │
│    │ 因子         │ 权重          │ 说明            │      │
│    ├──────────────┼───────────────┼─────────────────┤      │
│    │ 域名年龄     │ 高            │ 老域名更可信    │      │
│    │ 域名长度     │ 中            │ 短域名更珍贵    │      │
│    │ TLD类型      │ 中            │ .com > .xyz     │      │
│    │ 流量排名     │ 高            │ Alexa/Similar   │      │
│    │ SSL证书      │ 低            │ 基本要求        │      │
│    └──────────────┴───────────────┴─────────────────┘      │
│                                                             │
│  攻击分析：                                                  │
│    ❌ 可以批量注册便宜域名（.xyz 约$1/年）                   │
│    ✅ 高价值域名极难获取                                     │
│    ✅ DNS验证非常可靠                                        │
│                                                             │
│  综合评分：⭐⭐⭐ (3/5) - 单独使用价值有限                   │
│  适用人群：网站运营者、企业                                  │
│  建议：与其他抵押物组合使用                                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 2.2 加密货币地址

```
┌─────────────────────────────────────────────────────────────┐
│                  加密货币地址抵押物分析                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  验证方式：                                                  │
│    用钱包私钥签署消息: "I am AgentNetwork node abc123"       │
│    任何人可验证签名有效性                                    │
│                                                             │
│  支持的链：                                                  │
│    - Ethereum / EVM兼容链                                   │
│    - Bitcoin                                                │
│    - Solana                                                 │
│    - 其他支持消息签名的链                                    │
│                                                             │
│  价值评估因子：                                              │
│    ┌──────────────┬───────────────┬─────────────────┐      │
│    │ 因子         │ 权重          │ 说明            │      │
│    ├──────────────┼───────────────┼─────────────────┤      │
│    │ 地址年龄     │ 高            │ 首次交易时间    │      │
│    │ 交易次数     │ 中            │ 活跃度指标      │      │
│    │ 持有资产     │ 高            │ 当前余额        │      │
│    │ NFT持有      │ 中            │ 某些NFT有价值   │      │
│    │ DeFi参与     │ 中            │ 链上历史        │      │
│    │ ENS/域名     │ 高            │ 短ENS很珍贵     │      │
│    └──────────────┴───────────────┴─────────────────┘      │
│                                                             │
│  攻击分析：                                                  │
│    ❌ 地址免费创建                                           │
│    ✅ 链上历史无法伪造                                       │
│    ✅ 持有资产是真实的"抵押"                                 │
│    ⚠️ 需要实际查询链上数据                                  │
│                                                             │
│  综合评分：⭐⭐⭐⭐ (4/5)                                     │
│  适用人群：加密货币用户                                      │
│  优势：完全去中心化、隐私保护好                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 2.3 时间锁定代币（高级）

```
概念：用户锁定一定数量的代币到智能合约，锁定期间不能取出

优势：
- 真实的经济抵押
- 作恶会导致抵押物损失（可设计惩罚机制）
- 完全去中心化、无需信任

劣势：
- 需要用户持有代币
- 需要部署智能合约
- 复杂度较高

适用场景：高价值节点、超级节点候选
```

---

### Tier 3: 身份证明（高信任等级）

#### 3.1 企业/教育邮箱

```
┌─────────────────────────────────────────────────────────────┐
│                    特殊邮箱抵押物分析                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  高价值邮箱类型：                                            │
│    - 企业邮箱：@google.com, @microsoft.com, @alibaba.com    │
│    - 教育邮箱：@stanford.edu, @mit.edu, @tsinghua.edu.cn   │
│    - 政府邮箱：@gov.cn, @.gov                               │
│                                                             │
│  验证方式：                                                  │
│    发送验证邮件到该邮箱，用户点击确认链接                    │
│    或：用户用该邮箱发送包含签名的邮件                        │
│                                                             │
│  价值分析：                                                  │
│    ✅ 获取成本极高（需要在该组织工作/学习）                  │
│    ✅ 几乎无法伪造                                           │
│    ❌ 隐私问题（暴露工作单位）                               │
│    ❌ 邮箱可能被收回（离职/毕业）                            │
│                                                             │
│  综合评分：⭐⭐⭐⭐⭐ (5/5) - 信任度最高                      │
│  适用场景：需要高度信任的场合                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 3.2 政府认证（理论探讨）

```
可能的形式：
- 数字身份证（如中国的电子身份证）
- eID（欧盟）
- Aadhaar（印度）

问题：
- 严重的隐私问题
- 依赖政府基础设施
- 不同国家标准不同
- 与去中心化理念冲突

结论：暂不考虑
```

---

## 🏗️ 系统设计

### 架构概览

```
┌─────────────────────────────────────────────────────────────────┐
│                     抵押物信任系统架构                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                     Agent Layer                          │   │
│  │  ┌─────────────────────────────────────────────────────┐│   │
│  │  │  Agent 自主判断：                                    ││   │
│  │  │  - 我信任哪些类型的抵押物？                          ││   │
│  │  │  - 这个节点的抵押物组合是否足够？                    ││   │
│  │  │  - 我愿意和低抵押物节点交易吗？                      ││   │
│  │  └─────────────────────────────────────────────────────┘│   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   Collateral Engine                      │   │
│  │  ┌───────────┬───────────┬───────────┬───────────┐     │   │
│  │  │ Verifier  │ Evaluator │ Registry  │ Cache     │     │   │
│  │  │ 验证模块   │ 评估模块   │ 注册表    │ 缓存      │     │   │
│  │  └───────────┴───────────┴───────────┴───────────┘     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   External Adapters                      │   │
│  │  ┌─────────┬─────────┬─────────┬─────────┬─────────┐   │   │
│  │  │ Twitter │ GitHub  │   DNS   │Ethereum │  SMTP   │   │   │
│  │  │ Adapter │ Adapter │ Adapter │ Adapter │ Adapter │   │   │
│  │  └─────────┴─────────┴─────────┴─────────┴─────────┘   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 数据结构

```go
// 抵押物类型
type CollateralType string

const (
    CollateralTwitter    CollateralType = "twitter"
    CollateralGitHub     CollateralType = "github"
    CollateralLinkedIn   CollateralType = "linkedin"
    CollateralDomain     CollateralType = "domain"
    CollateralEthereum   CollateralType = "ethereum"
    CollateralBitcoin    CollateralType = "bitcoin"
    CollateralEmail      CollateralType = "email"
    CollateralMastodon   CollateralType = "mastodon"
)

// 单个抵押物
type Collateral struct {
    Type       CollateralType `json:"type"`
    Identifier string         `json:"identifier"` // @username, 域名, 地址等
    ProofURL   string         `json:"proof_url"`  // 证明链接
    Signature  string         `json:"signature"`  // 可选：链下签名
    
    // 验证结果
    Verified   bool           `json:"verified"`
    VerifiedAt time.Time      `json:"verified_at"`
    ExpiresAt  time.Time      `json:"expires_at"` // 验证有效期
    
    // 评估结果
    RawScore   float64        `json:"raw_score"`   // 原始分数
    Factors    map[string]interface{} `json:"factors"` // 评估因子详情
}

// 节点的抵押物集合
type CollateralBundle struct {
    NodeID       string        `json:"node_id"`
    Collaterals  []Collateral  `json:"collaterals"`
    
    // 综合评估
    TotalScore   float64       `json:"total_score"`
    TrustTier    int           `json:"trust_tier"`  // 0-4
    LastUpdated  time.Time     `json:"last_updated"`
}

// 抵押物验证请求
type VerificationRequest struct {
    Type       CollateralType `json:"type"`
    Identifier string         `json:"identifier"`
    ProofData  string         `json:"proof_data"` // 签名或验证码
}

// 评估配置（Agent可自定义）
type EvaluationConfig struct {
    // 权重配置
    TypeWeights map[CollateralType]float64 `json:"type_weights"`
    
    // 最低要求
    MinTotalScore float64 `json:"min_total_score"`
    RequiredTypes []CollateralType `json:"required_types"` // 必须有的类型
    
    // 组合加成
    ComboBonuses map[string]float64 `json:"combo_bonuses"`
}
```

### 验证流程

```
┌─────────────────────────────────────────────────────────────────┐
│                      Twitter 验证流程示例                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 节点发起绑定请求                                             │
│     POST /api/collateral/bind                                   │
│     {                                                           │
│       "type": "twitter",                                        │
│       "identifier": "@alice_agent"                              │
│     }                                                           │
│                                                                 │
│  2. 系统返回验证指令                                             │
│     {                                                           │
│       "challenge": "Verify: AgentNetwork node abc123...",       │
│       "instructions": "请在 Twitter 发布包含以下内容的推文"      │
│     }                                                           │
│                                                                 │
│  3. 用户在 Twitter 发布推文                                      │
│     "@alice_agent: Verify: AgentNetwork node abc123..."         │
│                                                                 │
│  4. 节点提交验证                                                 │
│     POST /api/collateral/verify                                 │
│     {                                                           │
│       "type": "twitter",                                        │
│       "identifier": "@alice_agent",                             │
│       "proof_url": "https://twitter.com/alice_agent/status/..." │
│     }                                                           │
│                                                                 │
│  5. 系统验证并评估                                               │
│     - 获取推文内容，验证 challenge 匹配                          │
│     - 获取账号信息，计算价值分数                                 │
│     - 存储抵押物记录                                             │
│                                                                 │
│  6. 返回结果                                                     │
│     {                                                           │
│       "verified": true,                                         │
│       "score": 85.5,                                            │
│       "factors": {                                              │
│         "account_age_years": 5,                                 │
│         "followers": 1200,                                      │
│         "verified": false,                                      │
│         "activity_score": 0.8                                   │
│       }                                                         │
│     }                                                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### API 设计

```yaml
# 抵押物管理 API
/api/collateral:
  
  # 绑定新抵押物（第一步：获取 challenge）
  POST /bind:
    request:
      type: string        # twitter, github, domain, etc.
      identifier: string  # @username, domain, address
    response:
      challenge: string   # 验证码
      instructions: string # 验证指引
      expires_at: timestamp

  # 提交验证（第二步：验证 challenge）
  POST /verify:
    request:
      type: string
      identifier: string
      proof_url: string   # 证明链接
      proof_data: string  # 额外数据（如签名）
    response:
      verified: bool
      score: float
      factors: object
      error: string

  # 解绑抵押物
  DELETE /{type}/{identifier}:
    response:
      success: bool

  # 查询节点抵押物
  GET /node/{node_id}:
    response:
      bundle: CollateralBundle
      
  # 刷新验证（抵押物可能过期）
  POST /refresh/{type}/{identifier}:
    response:
      verified: bool
      score: float

# Agent 查询 API
/api/trust:
  
  # 评估节点信任度
  GET /evaluate/{node_id}:
    query:
      config: string  # 可选：自定义评估配置
    response:
      total_score: float
      trust_tier: int
      collaterals: []Collateral
      recommendation: string  # "trusted", "cautious", "untrusted"
      
  # 批量评估
  POST /evaluate/batch:
    request:
      node_ids: []string
      config: object
    response:
      results: map[string]EvaluationResult
```

### 评分算法

```go
// 综合评分计算
func CalculateTotalScore(bundle *CollateralBundle, config *EvaluationConfig) float64 {
    var totalScore float64
    typesPresent := make(map[CollateralType]bool)
    
    // 1. 计算各抵押物加权分数
    for _, c := range bundle.Collaterals {
        if !c.Verified {
            continue
        }
        weight := config.TypeWeights[c.Type]
        if weight == 0 {
            weight = 1.0
        }
        totalScore += c.RawScore * weight
        typesPresent[c.Type] = true
    }
    
    // 2. 应用组合加成
    // 例如：同时有 GitHub + Twitter + 域名 = +20%
    for comboKey, bonus := range config.ComboBonuses {
        types := parseComboKey(comboKey) // "github+twitter+domain"
        if allPresent(typesPresent, types) {
            totalScore *= (1 + bonus)
        }
    }
    
    // 3. 多样性加成（鼓励多种类型）
    diversityBonus := math.Min(float64(len(typesPresent)) * 0.05, 0.2)
    totalScore *= (1 + diversityBonus)
    
    return totalScore
}

// 计算信任等级
func CalculateTrustTier(score float64) int {
    switch {
    case score >= 200:
        return 4 // 极高信任
    case score >= 100:
        return 3 // 高信任
    case score >= 50:
        return 2 // 中等信任
    case score >= 20:
        return 1 // 基础信任
    default:
        return 0 // 未验证
    }
}
```

---

## 🛡️ 安全考虑

### 攻击场景与防御

| 攻击 | 描述 | 防御 |
|-----|------|-----|
| **账号购买** | 购买老账号绑定 | 检查活跃度连续性、最近活动 |
| **刷量攻击** | 刷粉丝、刷Star | 关注互动率、质量而非数量 |
| **伪造证明** | 发假截图 | 必须实时 API 验证 |
| **盗用账号** | 盗取他人账号绑定 | 定期重新验证、异常检测 |
| **批量注册** | 批量注册便宜抵押物 | 单一类型分数有上限 |
| **API滥用** | 频繁调用验证 | 速率限制、验证冷却期 |

### 隐私保护

```
原则：最小暴露

1. 抵押物信息可选择性公开
   - 公开：抵押物类型 + 分数
   - 私有：具体 identifier

2. 支持零知识证明（高级）
   - 证明"我有 >1000 粉丝的 Twitter"
   - 不暴露具体是哪个账号

3. 验证缓存
   - 验证结果缓存，不需要每次都查询
   - 定期自动刷新
```

---

## 🔄 与现有系统集成

### 与 Task 35（门牌号）集成

```
注册门牌号时可以提交抵押物：

标准流程（有担保人）：
  门牌号 = 公钥 + 担保人签名 + 邻居见证

无担保人流程（用抵押物）：
  门牌号 = 公钥 + 抵押物证明（需达到最低分数）
  
混合流程（增强信任）：
  门牌号 = 公钥 + 担保人签名 + 抵押物证明
```

### 与声誉系统集成

```
抵押物影响初始声誉：

无抵押物新节点：
  初始声誉 = 10

有抵押物新节点：
  初始声誉 = 10 + min(抵押物分数 / 10, 40)
  
最高可达 50 初始声誉（vs 默认 10）
```

### 与配额系统集成

```
抵押物影响消息配额：

Trust Tier 0（无抵押）: 100 条/天
Trust Tier 1（基础）:   200 条/天  
Trust Tier 2（中等）:   500 条/天
Trust Tier 3（高）:    1000 条/天
Trust Tier 4（极高）:  2000 条/天
```

---

## 🤝 第二部分：信任传播机制 (Web of Trust)

> **"我是某某的朋友" - 熟人社会的数字化重建**

### 核心理念

```
┌─────────────────────────────────────────────────────────────────┐
│                     熟人社会的信任模型                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  物理世界：                                                      │
│    "你好，我是张三的朋友，他介绍我来的"                          │
│    → 你对我的信任 = f(你对张三的信任, 张三对我的背书强度)        │
│                                                                 │
│  数字世界（AgentNetwork）：                                      │
│    节点C拿着节点B的签名："B信任C"                                │
│    节点A看到后：我信任B，B信任C，所以C有一定可信度               │
│                                                                 │
│  关键约束：                                                      │
│    - 信任传播会衰减（距离越远，信任度越低）                      │
│    - 惩罚也会传播（你介绍的人作恶，你也受损）                    │
│    - 防止重放攻击（签名有时效性）                                │
│    - 防止回环攻击（A→B→C→A 不能无限循环增强）                   │
│                                                                 │
│  假设前提：                                                      │
│    "世上还是好人多，我们惩戒坏人"                                │
│    → 大多数人不会轻易为坏人背书                                  │
│    → 背书意味着承担责任                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 类比：PGP 信任网络

```
PGP (Pretty Good Privacy) 的 Web of Trust：

1. 我亲自见过 Alice，验证了她的密钥，我签署她的公钥
2. Alice 见过 Bob，签署了 Bob 的公钥
3. 我收到 Bob 的消息，虽然没见过他，但：
   - 我信任 Alice（我亲自签署）
   - Alice 信任 Bob（Alice 签署）
   - 所以 Bob 有一定可信度

AgentNetwork 的扩展：
- 不仅传递信任，还传递惩罚
- 签名有时效性，需要定期更新
- 可以指定信任程度（完全信任 vs 边缘信任）
```

### 信任传播数据结构

```go
// 信任声明 - 节点主人发起的信任表达
type TrustEndorsement struct {
    // 基本信息
    EndorserID    string    `json:"endorser_id"`    // 背书人节点ID
    EndorseeID    string    `json:"endorsee_id"`    // 被背书人节点ID
    
    // 信任程度
    TrustLevel    TrustLevel `json:"trust_level"`   // 信任等级
    Relationship  string     `json:"relationship"`   // 可选：关系描述
    
    // 时效性
    IssuedAt      time.Time  `json:"issued_at"`     // 签发时间
    ExpiresAt     time.Time  `json:"expires_at"`    // 过期时间
    Nonce         string     `json:"nonce"`         // 防重放随机数
    
    // 签名
    Signature     string     `json:"signature"`     // 背书人私钥签名
}

// 信任等级
type TrustLevel int

const (
    TrustLevelUnknown    TrustLevel = 0  // 未知
    TrustLevelMarginal   TrustLevel = 1  // 边缘信任（网络朋友）
    TrustLevelPartial    TrustLevel = 2  // 部分信任（熟人）
    TrustLevelFull       TrustLevel = 3  // 完全信任（线下见面）
    TrustLevelUltimate   TrustLevel = 4  // 终极信任（自己的其他节点）
)

// 信任路径 - A 到 C 的信任链
type TrustPath struct {
    From          string              `json:"from"`          // 起点节点
    To            string              `json:"to"`            // 终点节点
    Path          []TrustEndorsement  `json:"path"`          // 信任链
    
    // 计算结果
    PathLength    int                 `json:"path_length"`   // 路径长度
    EffectiveTrust float64           `json:"effective_trust"` // 有效信任度（衰减后）
    
    // 验证状态
    Validated     bool                `json:"validated"`     // 所有签名是否有效
    ValidatedAt   time.Time           `json:"validated_at"`
}

// 信任图谱 - 节点维护的信任关系
type TrustGraph struct {
    NodeID        string                      `json:"node_id"`
    
    // 我直接信任的人（我签署的）
    DirectTrust   map[string]*TrustEndorsement `json:"direct_trust"`
    
    // 信任我的人（他们签署给我的）
    TrustedBy     map[string]*TrustEndorsement `json:"trusted_by"`
    
    // 缓存的信任路径
    PathCache     map[string]*TrustPath        `json:"path_cache"`
}
```

### 信任传播算法

```go
// 信任衰减公式
func CalculateEffectiveTrust(path *TrustPath) float64 {
    if len(path.Path) == 0 {
        return 0
    }
    
    // 初始信任度 = 第一跳的信任等级
    trust := float64(path.Path[0].TrustLevel) / float64(TrustLevelUltimate)
    
    // 每一跳信任度衰减
    decayFactor := 0.7  // 每跳衰减 30%
    
    for i := 1; i < len(path.Path); i++ {
        hopTrust := float64(path.Path[i].TrustLevel) / float64(TrustLevelUltimate)
        trust = trust * decayFactor * hopTrust
    }
    
    // 路径长度惩罚（超过3跳快速衰减）
    if len(path.Path) > 3 {
        extraHops := len(path.Path) - 3
        trust *= math.Pow(0.5, float64(extraHops))
    }
    
    return trust
}

// 示例：
// A --[Full]--> B --[Full]--> C --[Partial]--> D
// 
// A对B: 3/4 = 0.75
// A对C: 0.75 * 0.7 * 0.75 = 0.39
// A对D: 0.39 * 0.7 * 0.5 = 0.14
```

### 信任建立流程

```
┌─────────────────────────────────────────────────────────────────┐
│                      信任建立流程                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  场景：Alice 和 Bob 线下见面，互相建立信任                       │
│                                                                 │
│  Step 1: 线下验证                                               │
│    Alice 和 Bob 见面，交换节点ID和公钥                          │
│    双方确认对方身份（比如查看彼此的抵押物证明）                  │
│                                                                 │
│  Step 2: Alice 签署信任声明                                     │
│    {                                                            │
│      "endorser_id": "alice_node_id",                           │
│      "endorsee_id": "bob_node_id",                             │
│      "trust_level": 3,  // Full Trust                          │
│      "relationship": "线下朋友",                                │
│      "issued_at": "2026-02-04T10:00:00Z",                      │
│      "expires_at": "2027-02-04T10:00:00Z",  // 一年有效         │
│      "nonce": "random_string",                                 │
│      "signature": "alice_sign(...)"                            │
│    }                                                            │
│                                                                 │
│  Step 3: Bob 存储 Alice 的背书                                  │
│    Bob 可以向其他节点展示："Alice 信任我"                       │
│                                                                 │
│  Step 4: Carol 查询 Bob 的信任链                                │
│    Carol 信任 Alice                                             │
│    Carol 看到 Alice 信任 Bob                                    │
│    → Carol 对 Bob 有衍生信任                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 惩罚传播机制

```
┌─────────────────────────────────────────────────────────────────┐
│                      惩罚传播机制                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  核心理念：                                                      │
│    "你介绍了一个坏朋友给我，你的声誉也要下降"                    │
│                                                                 │
│  场景：A 信任 B，B 信任 C，C 作恶被惩罚                          │
│                                                                 │
│  惩罚计算：                                                      │
│    C 的惩罚 = BasePenalty (例如 -50 声誉)                       │
│    B 的惩罚 = C的惩罚 × 衰减系数 × B对C的信任度                 │
│             = -50 × 0.5 × 0.75 = -18.75                        │
│    A 的惩罚 = B的惩罚 × 衰减系数 × A对B的信任度                 │
│             = -18.75 × 0.5 × 0.75 = -7.03                      │
│                                                                 │
│  惩罚传播规则：                                                  │
│    1. 只向背书方向传播（谁签署谁受损）                          │
│    2. 每跳衰减 50%                                              │
│    3. 最多传播 3 跳                                             │
│    4. 有最低阈值（< 1 声誉不传播）                              │
│                                                                 │
│  自我保护：                                                      │
│    - 如果 B 主动撤销对 C 的信任，惩罚减半                       │
│    - 撤销必须在 C 作恶之前                                      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

```go
// 惩罚传播数据结构
type PunishmentPropagation struct {
    // 作恶者信息
    OffenderID    string    `json:"offender_id"`
    OffenseType   string    `json:"offense_type"`   // spam, fraud, etc.
    BasePenalty   float64   `json:"base_penalty"`   // 基础惩罚
    
    // 传播结果
    Propagations  []PropagationRecord `json:"propagations"`
}

type PropagationRecord struct {
    NodeID        string    `json:"node_id"`        // 受影响节点
    Depth         int       `json:"depth"`          // 距离作恶者的跳数
    Penalty       float64   `json:"penalty"`        // 实际惩罚
    TrustLevel    int       `json:"trust_level"`    // 该节点对下游的信任等级
    Reason        string    `json:"reason"`         // "endorsed offender at depth N"
}

// 惩罚传播算法
func PropagatePunishment(
    offenderID string,
    basePenalty float64,
    trustGraph *GlobalTrustGraph,
) *PunishmentPropagation {
    
    result := &PunishmentPropagation{
        OffenderID:  offenderID,
        BasePenalty: basePenalty,
    }
    
    decayFactor := 0.5
    maxDepth := 3
    minPenalty := 1.0
    
    // BFS 遍历信任反向图
    queue := []struct {
        nodeID  string
        depth   int
        penalty float64
    }{{offenderID, 0, basePenalty}}
    
    visited := make(map[string]bool)
    
    for len(queue) > 0 {
        current := queue[0]
        queue = queue[1:]
        
        if visited[current.nodeID] || current.depth > maxDepth {
            continue
        }
        visited[current.nodeID] = true
        
        // 找到所有为当前节点背书的人
        endorsers := trustGraph.GetEndorsersOf(current.nodeID)
        
        for _, endorser := range endorsers {
            trustLevel := float64(endorser.TrustLevel) / float64(TrustLevelUltimate)
            propagatedPenalty := current.penalty * decayFactor * trustLevel
            
            if propagatedPenalty < minPenalty {
                continue
            }
            
            result.Propagations = append(result.Propagations, PropagationRecord{
                NodeID:     endorser.EndorserID,
                Depth:      current.depth + 1,
                Penalty:    propagatedPenalty,
                TrustLevel: int(endorser.TrustLevel),
                Reason:     fmt.Sprintf("endorsed offender at depth %d", current.depth),
            })
            
            queue = append(queue, struct {
                nodeID  string
                depth   int
                penalty float64
            }{endorser.EndorserID, current.depth + 1, propagatedPenalty})
        }
    }
    
    return result
}
```

### 安全机制

#### 防止重放攻击

```go
// 信任声明包含时效性和唯一标识
type TrustEndorsement struct {
    // ... 其他字段
    
    IssuedAt   time.Time `json:"issued_at"`   // 签发时间
    ExpiresAt  time.Time `json:"expires_at"`  // 过期时间（建议1年）
    Nonce      string    `json:"nonce"`       // 唯一随机数
}

// 验证信任声明
func ValidateEndorsement(e *TrustEndorsement) error {
    // 1. 检查时效性
    if time.Now().After(e.ExpiresAt) {
        return errors.New("endorsement expired")
    }
    
    // 2. 检查 nonce 是否已使用（防重放）
    if nonceCache.Contains(e.Nonce) {
        return errors.New("nonce already used")
    }
    nonceCache.Add(e.Nonce, e.ExpiresAt)
    
    // 3. 验证签名
    if !VerifySignature(e.EndorserID, e.Signature, e.SigningPayload()) {
        return errors.New("invalid signature")
    }
    
    return nil
}
```

#### 防止回环攻击

```go
// 回环攻击示例：
// A → B → C → A → B → C → ...
// 如果不防护，信任度会无限累加

// 防护措施
func FindTrustPaths(from, to string, graph *TrustGraph) []*TrustPath {
    var paths []*TrustPath
    visited := make(map[string]bool)
    
    var dfs func(current string, path []TrustEndorsement)
    dfs = func(current string, path []TrustEndorsement) {
        // 1. 检测回环
        if visited[current] {
            return
        }
        visited[current] = true
        defer func() { visited[current] = false }()
        
        // 2. 限制最大路径长度
        if len(path) > MaxPathLength {
            return
        }
        
        // 3. 到达目标
        if current == to && len(path) > 0 {
            paths = append(paths, &TrustPath{
                From: from,
                To:   to,
                Path: append([]TrustEndorsement{}, path...),
            })
            return
        }
        
        // 4. 继续搜索
        for _, endorsement := range graph.DirectTrust[current] {
            dfs(endorsement.EndorseeID, append(path, *endorsement))
        }
    }
    
    dfs(from, nil)
    return paths
}

// 信任计算时，同一节点只计算一次
func CalculateTotalTrust(from, to string, paths []*TrustPath) float64 {
    // 取最短/最强路径，而非累加
    maxTrust := 0.0
    for _, path := range paths {
        trust := CalculateEffectiveTrust(path)
        if trust > maxTrust {
            maxTrust = trust
        }
    }
    return maxTrust
}
```

### API 设计

```yaml
# 信任传播 API
/api/trust:
  
  # 发起信任声明（我信任某人）
  POST /endorse:
    request:
      endorsee_id: string      # 被信任节点
      trust_level: int         # 1-4
      relationship: string     # 可选描述
      expires_in: duration     # 有效期，默认1年
    response:
      endorsement: TrustEndorsement
      
  # 撤销信任声明
  DELETE /endorse/{endorsee_id}:
    response:
      success: bool
      revoked_at: timestamp
      
  # 查询我的信任列表
  GET /my-endorsements:
    response:
      direct_trust: []TrustEndorsement    # 我信任的
      trusted_by: []TrustEndorsement      # 信任我的
      
  # 查询两个节点间的信任路径
  GET /path:
    query:
      from: string
      to: string
    response:
      paths: []TrustPath
      best_path: TrustPath
      effective_trust: float
      
  # 查询节点的信任图谱
  GET /graph/{node_id}:
    query:
      depth: int  # 展开深度，默认2
    response:
      graph: TrustGraphView
      
# Agent 查询 API（综合抵押物 + 信任传播）
/api/trust/evaluate/{target_node_id}:
  GET:
    response:
      # 抵押物评分
      collateral_score: float
      collateral_tier: int
      collaterals: []Collateral
      
      # 信任传播评分
      trust_score: float
      trust_paths: []TrustPathSummary
      
      # 综合评分
      combined_score: float
      recommendation: string  # "high_trust", "moderate", "low_trust", "unknown"
      
      # Agent 可读的信任解释
      trust_explanation: string
      # 例如："该节点有 GitHub(5年活跃) + Twitter(认证账号)，
      #        且是你信任的 Alice 的朋友（Alice 给予完全信任）"
```

### Agent 视角：简单自然的信任查询

```
┌─────────────────────────────────────────────────────────────────┐
│                   Agent 的信任查询体验                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Agent 问："这个节点可信吗？"                                   │
│                                                                 │
│  系统返回：                                                      │
│  {                                                              │
│    "node_id": "xyz789",                                        │
│    "combined_score": 78.5,                                      │
│    "recommendation": "moderate_trust",                          │
│    "trust_explanation": "                                       │
│      📋 抵押物：                                                │
│        - GitHub: 3年活跃开发者，500+ commits                    │
│        - 无其他抵押物                                           │
│                                                                 │
│      🤝 信任链：                                                │
│        - 你 ← [完全信任] ← Alice ← [部分信任] ← xyz789         │
│        - 有效信任度: 0.39                                       │
│                                                                 │
│      ⚠️ 注意事项：                                              │
│        - 该节点加入网络仅 3 天                                  │
│        - 建议小额任务先试验                                     │
│    "                                                            │
│  }                                                              │
│                                                                 │
│  Agent 自主决策："好的，我愿意接受这个节点的小型任务请求"       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📅 实现路径

### Phase 1: 核心框架（P0）
- [ ] 抵押物数据结构
- [ ] 验证引擎框架
- [ ] 评分算法

### Phase 2: 主要适配器（P0）
- [ ] Twitter 适配器
- [ ] GitHub 适配器
- [ ] 以太坊签名适配器

### Phase 3: 信任传播（P1）★ 新增
- [ ] 信任声明数据结构
- [ ] 信任路径查找算法
- [ ] 信任衰减计算
- [ ] 防重放/防回环机制
- [ ] 惩罚传播机制

### Phase 4: 扩展适配器（P1）
- [ ] DNS/域名适配器
- [ ] LinkedIn 适配器
- [ ] 邮箱验证适配器

### Phase 5: 高级功能（P2）
- [ ] 零知识证明支持
- [ ] 时间锁定代币支持
- [ ] 跨链资产验证
- [ ] 信任图谱可视化

---

## 💡 设计哲学总结

> **信任是"信号"，不是"门槛"**

### 双重信任锚点

| 信任来源 | 特点 | 适用场景 |
|---------|------|---------|
| **抵押物** | 物理世界沉没成本 | 陌生人初次接触 |
| **信任传播** | 熟人社会背书 | 有共同朋友时 |
| **组合** | 最强信号 | 高价值任务 |

### 核心原则

1. **Agent 自主判断** - 系统只提供数据和解释，Agent 决定是否信任
2. **责任连带** - 背书意味着责任，降低轻率背书的动机
3. **信任衰减** - 距离越远，信任越弱，符合现实直觉
4. **惩罚传播** - 作恶有代价，且代价会扩散，形成社会约束
5. **好人假设** - 相信大多数人是善意的，系统设计为惩戒少数坏人

> **物理世界的"熟人社会"是信任的基础**  
> **AgentNetwork 在数字世界重建这种社会关系网络**

---

## 🔗 附录：与 PGP Web of Trust 的对比

| 特性 | PGP | AgentNetwork |
|-----|-----|--------------|
| 信任对象 | 公钥 | 节点身份 |
| 信任传播 | ✅ | ✅ |
| 信任衰减 | 手动设置 | 自动计算 |
| 惩罚传播 | ❌ | ✅ |
| 时效性 | 可选 | 强制 |
| 防重放 | 依赖时间戳 | Nonce + 时间戳 |
| 信任等级 | 3级 | 4级 |
| Agent可读 | ❌ | ✅ 提供自然语言解释 |

---

## 📊 附录：声誉边界设计

> **声誉是否有上限或下限？**

### 深度思考

这个问题需要从多个角度考虑：

```
┌─────────────────────────────────────────────────────────────────┐
│                      声誉边界的权衡                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  物理世界的声誉特点：                                            │
│    - 理论上无上限（但实际有边际递减）                            │
│    - 可以变为"臭名昭著"（负声誉）                                │
│    - 重建声誉需要很长时间                                        │
│    - 高声誉者作恶代价极大（身败名裂）                            │
│                                                                 │
│  数字世界的特殊性：                                              │
│    - 数值需要有边界（防止溢出、便于计算）                        │
│    - 需要防止"寡头化"（老节点垄断信任）                          │
│    - 需要给新人机会（社会流动性）                                │
│    - 需要防止"破罐破摔"（声誉太低就躺平）                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 设计方案

```
声誉范围：[0, 1000]

┌─────────────────────────────────────────────────────────────────┐
│                        声誉刻度尺                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  0 ────────── 100 ────────── 500 ────────── 1000               │
│  │             │              │               │                 │
│  │             │              │               │                 │
│  ▼             ▼              ▼               ▼                 │
│  黑名单        新人           活跃者          元老               │
│  (受限)        (初始10)       (正常)          (上限)             │
│                                                                 │
│  ════════════════════════════════════════════════════════════   │
│                                                                 │
│  区间定义：                                                      │
│  ┌────────────┬─────────────┬───────────────────────────────┐  │
│  │ 区间       │ 等级名称     │ 权限                          │  │
│  ├────────────┼─────────────┼───────────────────────────────┤  │
│  │ 0-10       │ 黑名单       │ 严重受限，只能接收不能发送    │  │
│  │ 10-50      │ 观察期       │ 限制配额，不能担保他人        │  │
│  │ 50-200     │ 正常节点     │ 标准权限                      │  │
│  │ 200-500    │ 活跃节点     │ 可担保新人，较高配额          │  │
│  │ 500-800    │ 信任节点     │ 可参与委员会，高配额          │  │
│  │ 800-1000   │ 元老节点     │ 最高权限，可提名超级节点      │  │
│  └────────────┴─────────────┴───────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 为什么选择 [0, 1000]？

#### 下限为 0（而非负数）

```
理由：
1. 简化计算 - 不需要处理负数逻辑
2. 0 已经是"死亡"状态 - 声誉为0的节点几乎被全网忽略
3. 给重建机会 - 允许从0开始慢慢恢复
4. 防止"躺平" - 如果可以无限负，坏人可能彻底不在乎

替代方案（如果需要"污点记录"）：
- 声誉可以到0，但维护独立的"违规记录"
- 违规记录永久保存，即使声誉恢复
- Agent可以查询："这个节点曾经声誉归零过吗？"
```

#### 上限为 1000（而非无限）

```
理由：
1. 防止寡头化
   - 无上限 → 老节点声誉无限高 → 新人永远追不上
   - 有上限 → 到达上限后，大家在同一起跑线

2. 边际递减（现实模拟）
   - 现实中，声誉到一定程度后，增益递减
   - 马云和马化腾的"信任度"差别不大

3. 高声誉者作恶代价依然很大
   - 从1000掉到100，损失巨大
   - 上限不影响惩罚机制的威慑力

4. 鼓励持续贡献
   - 声誉会自然衰减（不活跃就下降）
   - 即使到达上限，也需要持续贡献维持
```

### 声誉增减规则

```go
// 声誉边界常量
const (
    ReputationMin     = 0
    ReputationMax     = 1000
    ReputationInitial = 10   // 新节点初始声誉
)

// 声誉变化公式
type ReputationChange struct {
    BaseAmount   float64  // 基础变化量
    CurrentRep   float64  // 当前声誉
}

// 获取声誉（边际递减）
func (r *ReputationChange) CalculateGain() float64 {
    // 越接近上限，获取越难
    // 公式：实际获取 = 基础量 × (1 - 当前声誉/上限)^0.5
    diminishingFactor := math.Pow(1 - r.CurrentRep/ReputationMax, 0.5)
    gain := r.BaseAmount * diminishingFactor
    
    // 确保不超过上限
    newRep := r.CurrentRep + gain
    if newRep > ReputationMax {
        return ReputationMax - r.CurrentRep
    }
    return gain
}

// 损失声誉（越低损失越小）
func (r *ReputationChange) CalculateLoss() float64 {
    // 越接近下限，损失越小（防止"踢皮球"）
    // 但高声誉者损失完整
    // 公式：实际损失 = 基础量 × (当前声誉/上限)^0.3
    scaleFactor := math.Pow(r.CurrentRep/ReputationMax, 0.3)
    loss := r.BaseAmount * scaleFactor
    
    // 确保不低于下限
    newRep := r.CurrentRep - loss
    if newRep < ReputationMin {
        return r.CurrentRep - ReputationMin
    }
    return loss
}
```

### 声誉变化示例

```
场景1：新人完成任务获得 +10 声誉
  当前声誉: 10
  基础奖励: 10
  递减因子: (1 - 10/1000)^0.5 = 0.995
  实际获得: 10 × 0.995 = 9.95
  新声誉: 19.95

场景2：元老完成同样任务
  当前声誉: 900
  基础奖励: 10
  递减因子: (1 - 900/1000)^0.5 = 0.316
  实际获得: 10 × 0.316 = 3.16
  新声誉: 903.16

场景3：新人作恶被惩罚 -50 声誉
  当前声誉: 50
  基础惩罚: 50
  缩放因子: (50/1000)^0.3 = 0.35
  实际损失: 50 × 0.35 = 17.5
  新声誉: 32.5

场景4：元老作恶被惩罚 -50 声誉
  当前声誉: 900
  基础惩罚: 50
  缩放因子: (900/1000)^0.3 = 0.97
  实际损失: 50 × 0.97 = 48.5
  新声誉: 851.5

关键洞察：
- 新人获取声誉容易，元老获取困难（边际递减）
- 新人损失小，元老损失大（高声誉者作恶代价更大）
- 这符合"德高望重"的现实逻辑
```

### 自然衰减机制

```go
// 声誉自然衰减（鼓励持续活跃）
func DecayReputation(currentRep float64, daysSinceLastActivity int) float64 {
    if daysSinceLastActivity <= 7 {
        return currentRep  // 7天内不衰减
    }
    
    // 每周衰减 1%，最多衰减到 50
    decayRate := 0.01 * float64((daysSinceLastActivity - 7) / 7)
    decayRate = math.Min(decayRate, 0.1)  // 单次最多衰减10%
    
    newRep := currentRep * (1 - decayRate)
    
    // 衰减不会低于 50（给老用户基础保护）
    if newRep < 50 && currentRep >= 50 {
        return 50
    }
    
    return newRep
}
```

### 与信任传播的集成

```
信任传播中的声誉影响：

1. 背书权重
   低声誉节点（<50）的背书权重 × 0.5
   高声誉节点（>500）的背书权重 × 1.2
   
2. 惩罚传播
   惩罚传播时，按声誉比例分担
   高声誉的介绍人承担更多责任（因为他的背书更有分量）
   
3. 担保资格
   声誉 < 200：不能为他人担保
   声誉 200-500：可担保 1 人
   声誉 > 500：可担保 3 人
```

### Agent 可读的声誉解释

```json
{
  "node_id": "xyz789",
  "reputation": {
    "current": 345,
    "tier": "活跃节点",
    "percentile": "top 25%",  // 全网排名
    "trend": "上升",          // 最近30天趋势
    "history": {
      "highest": 400,
      "lowest": 50,
      "violations": 0         // 归零次数
    }
  },
  "explanation": "该节点声誉 345，属于活跃节点，高于全网 75% 的节点。最近30天声誉上升，无违规记录。"
}
```

### 设计哲学

> **声誉是"社会资本"，需要长期积累，一旦失去很难恢复**

| 原则 | 设计 | 目的 |
|-----|------|-----|
| **有限资源** | 上限 1000 | 防止寡头、保持流动性 |
| **边际递减** | 越高越难涨 | 模拟现实、鼓励新人 |
| **高位高责** | 越高损失越大 | 约束高声誉者行为 |
| **最低保护** | 衰减底线 50 | 给老用户基础信任 |
| **可恢复** | 下限 0 | 给改过机会 |
| **持续激励** | 自然衰减 | 鼓励持续贡献 |
